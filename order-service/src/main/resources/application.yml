server:
  port: ${ORDER_SERVICE_PORT}
  forward-headers-strategy: native

spring:
  application:
    name: order-service

  profiles:
    active: dev
  boot:
    admin:
      client:
        url: http://localhost:8080
        instance:
          service-url: http://localhost:8080
          name: order-service

  devtools:
    restart:
      enabled: true
      exclude: static/**,public/**
      poll-interval: 1000
      quiet-period: 400
    livereload:
      enabled: true

  cache:
    type: redis

  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/drugstore_orders}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
    clean-disabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"
  endpoint:
    health:
      show-details: always
      show-components: always
    info:
      enabled: true
    metrics:
      enabled: true
    env:
      enabled: true
    loggers:
      enabled: true
  info:
    env:
      enabled: true
  health:
    db:
      enabled: true
    diskspace:
      enabled: true
    mail:
      enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
    doc-expansion: none
  packages-to-scan: io.github.alexisTrejo11.drugstore.order
  paths-to-match: /api/**
  cache:
    disabled: true

app:
  redis:
    key-prefix: ${REDIS_KEY_PREFIX:drugstore:order:}
  rate-limit:
    global:
      enabled: true
      maxRequests: 5000
      durationHours: 1
    profiles:
      standard:
        maxRequests: 100
        durationSeconds: 60
      sensitive:
        maxRequests: 5
        durationSeconds: 300
      public:
        maxRequests: 1000
        durationSeconds: 60
      admin:
        maxRequests: 50
        durationSeconds: 1
      customer-read:
        maxRequests: 100
        durationSeconds: 60
      customer-write:
        maxRequests: 20
        durationSeconds: 60

logging:
    name: logs/order-service.log
    path: logs
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
    io.github.alexisTrejo11.drugstore: DEBUG
    libs_kernel.log.audit: INFO

audit:
  log:
    enabled: true
    excluded-endpoints: /actuator/health, /actuator/info, /swagger-ui.html, /v3/api-docs

jwt:
  secret: ${JWT_SECRET_KEY}
  access-token-expiration-minutes: ${JWT_ACCESS_TOKEN_EXPIRATION_MINUTES:15}
  refresh-token-expiration-minutes: ${JWT_REFRESH_TOKEN_EXPIRATION_MINUTES:43200} # 30 days
  issuer: ${JWT_ISSUER}
